// *** WARNING: this file was generated by pulumi-java-gen. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.volcengine.volcenginecc.alb.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class RuleRedirectConfigArgs extends com.pulumi.resources.ResourceArgs {

    public static final RuleRedirectConfigArgs Empty = new RuleRedirectConfigArgs();

    /**
     * 重定向的域名。若创建/修改重定向类型的转发规则时，重定向域名设置为空，接口会返回${host}，该变量含义为重定向域名与请求域名保持一致，但不支持创建/修改时，将重定向域名设置为${host}.。
     * 
     */
    @Import(name="redirectDomain")
    private @Nullable Output<String> redirectDomain;

    /**
     * @return 重定向的域名。若创建/修改重定向类型的转发规则时，重定向域名设置为空，接口会返回${host}，该变量含义为重定向域名与请求域名保持一致，但不支持创建/修改时，将重定向域名设置为${host}.。
     * 
     */
    public Optional<Output<String>> redirectDomain() {
        return Optional.ofNullable(this.redirectDomain);
    }

    /**
     * 重定向状态码。301、302、307、308。
     * 
     */
    @Import(name="redirectHttpCode")
    private @Nullable Output<String> redirectHttpCode;

    /**
     * @return 重定向状态码。301、302、307、308。
     * 
     */
    public Optional<Output<String>> redirectHttpCode() {
        return Optional.ofNullable(this.redirectHttpCode);
    }

    /**
     * 重定向的端口。若创建/修改重定向类型的转发规则时，重定向端口设置为空，接口会返回${port}，该变量含义为重定向端口与请求端口（监听器端口）保持一致，但不支持创建/修改时，将重定向端口设置为${port}.。
     * 
     */
    @Import(name="redirectPort")
    private @Nullable Output<String> redirectPort;

    /**
     * @return 重定向的端口。若创建/修改重定向类型的转发规则时，重定向端口设置为空，接口会返回${port}，该变量含义为重定向端口与请求端口（监听器端口）保持一致，但不支持创建/修改时，将重定向端口设置为${port}.。
     * 
     */
    public Optional<Output<String>> redirectPort() {
        return Optional.ofNullable(this.redirectPort);
    }

    /**
     * 重定向使用的协议。HTTP、HTTPS。
     * 
     */
    @Import(name="redirectProtocol")
    private @Nullable Output<String> redirectProtocol;

    /**
     * @return 重定向使用的协议。HTTP、HTTPS。
     * 
     */
    public Optional<Output<String>> redirectProtocol() {
        return Optional.ofNullable(this.redirectProtocol);
    }

    /**
     * 重定向的URI。若创建/修改重定向类型的转发规则时，重定向uri设置为空，接口会返回${request*uri}，该变量含义为重定向uri与请求uri保持一致，但不支持创建/修改时，将重定向uri设置为${request*uri}.。
     * 
     */
    @Import(name="redirectUri")
    private @Nullable Output<String> redirectUri;

    /**
     * @return 重定向的URI。若创建/修改重定向类型的转发规则时，重定向uri设置为空，接口会返回${request*uri}，该变量含义为重定向uri与请求uri保持一致，但不支持创建/修改时，将重定向uri设置为${request*uri}.。
     * 
     */
    public Optional<Output<String>> redirectUri() {
        return Optional.ofNullable(this.redirectUri);
    }

    private RuleRedirectConfigArgs() {}

    private RuleRedirectConfigArgs(RuleRedirectConfigArgs $) {
        this.redirectDomain = $.redirectDomain;
        this.redirectHttpCode = $.redirectHttpCode;
        this.redirectPort = $.redirectPort;
        this.redirectProtocol = $.redirectProtocol;
        this.redirectUri = $.redirectUri;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(RuleRedirectConfigArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private RuleRedirectConfigArgs $;

        public Builder() {
            $ = new RuleRedirectConfigArgs();
        }

        public Builder(RuleRedirectConfigArgs defaults) {
            $ = new RuleRedirectConfigArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param redirectDomain 重定向的域名。若创建/修改重定向类型的转发规则时，重定向域名设置为空，接口会返回${host}，该变量含义为重定向域名与请求域名保持一致，但不支持创建/修改时，将重定向域名设置为${host}.。
         * 
         * @return builder
         * 
         */
        public Builder redirectDomain(@Nullable Output<String> redirectDomain) {
            $.redirectDomain = redirectDomain;
            return this;
        }

        /**
         * @param redirectDomain 重定向的域名。若创建/修改重定向类型的转发规则时，重定向域名设置为空，接口会返回${host}，该变量含义为重定向域名与请求域名保持一致，但不支持创建/修改时，将重定向域名设置为${host}.。
         * 
         * @return builder
         * 
         */
        public Builder redirectDomain(String redirectDomain) {
            return redirectDomain(Output.of(redirectDomain));
        }

        /**
         * @param redirectHttpCode 重定向状态码。301、302、307、308。
         * 
         * @return builder
         * 
         */
        public Builder redirectHttpCode(@Nullable Output<String> redirectHttpCode) {
            $.redirectHttpCode = redirectHttpCode;
            return this;
        }

        /**
         * @param redirectHttpCode 重定向状态码。301、302、307、308。
         * 
         * @return builder
         * 
         */
        public Builder redirectHttpCode(String redirectHttpCode) {
            return redirectHttpCode(Output.of(redirectHttpCode));
        }

        /**
         * @param redirectPort 重定向的端口。若创建/修改重定向类型的转发规则时，重定向端口设置为空，接口会返回${port}，该变量含义为重定向端口与请求端口（监听器端口）保持一致，但不支持创建/修改时，将重定向端口设置为${port}.。
         * 
         * @return builder
         * 
         */
        public Builder redirectPort(@Nullable Output<String> redirectPort) {
            $.redirectPort = redirectPort;
            return this;
        }

        /**
         * @param redirectPort 重定向的端口。若创建/修改重定向类型的转发规则时，重定向端口设置为空，接口会返回${port}，该变量含义为重定向端口与请求端口（监听器端口）保持一致，但不支持创建/修改时，将重定向端口设置为${port}.。
         * 
         * @return builder
         * 
         */
        public Builder redirectPort(String redirectPort) {
            return redirectPort(Output.of(redirectPort));
        }

        /**
         * @param redirectProtocol 重定向使用的协议。HTTP、HTTPS。
         * 
         * @return builder
         * 
         */
        public Builder redirectProtocol(@Nullable Output<String> redirectProtocol) {
            $.redirectProtocol = redirectProtocol;
            return this;
        }

        /**
         * @param redirectProtocol 重定向使用的协议。HTTP、HTTPS。
         * 
         * @return builder
         * 
         */
        public Builder redirectProtocol(String redirectProtocol) {
            return redirectProtocol(Output.of(redirectProtocol));
        }

        /**
         * @param redirectUri 重定向的URI。若创建/修改重定向类型的转发规则时，重定向uri设置为空，接口会返回${request*uri}，该变量含义为重定向uri与请求uri保持一致，但不支持创建/修改时，将重定向uri设置为${request*uri}.。
         * 
         * @return builder
         * 
         */
        public Builder redirectUri(@Nullable Output<String> redirectUri) {
            $.redirectUri = redirectUri;
            return this;
        }

        /**
         * @param redirectUri 重定向的URI。若创建/修改重定向类型的转发规则时，重定向uri设置为空，接口会返回${request*uri}，该变量含义为重定向uri与请求uri保持一致，但不支持创建/修改时，将重定向uri设置为${request*uri}.。
         * 
         * @return builder
         * 
         */
        public Builder redirectUri(String redirectUri) {
            return redirectUri(Output.of(redirectUri));
        }

        public RuleRedirectConfigArgs build() {
            return $;
        }
    }

}
