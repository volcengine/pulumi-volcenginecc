// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Data Source schema for Volcengine::ALB::HealthCheckTemplate
 */
export function getHealthCheckTemplate(args: GetHealthCheckTemplateArgs, opts?: pulumi.InvokeOptions): Promise<GetHealthCheckTemplateResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("volcenginecc:alb/getHealthCheckTemplate:getHealthCheckTemplate", {
        "id": args.id,
    }, opts);
}

/**
 * A collection of arguments for invoking getHealthCheckTemplate.
 */
export interface GetHealthCheckTemplateArgs {
    /**
     * Uniquely identifies the resource.
     */
    id: string;
}

/**
 * A collection of values returned by getHealthCheckTemplate.
 */
export interface GetHealthCheckTemplateResult {
    /**
     * 健康检查模版的创建时间。
     */
    readonly createTime: string;
    /**
     * 健康检查描述。不能以http://或https://开头。必须以字母或中文开头，可包含数字、英文逗号（,）、点号（.）、下划线（_）、空格（ ）、等号（=）、短横线（-）、中文逗号（，）、中文句号（。） 。长度限制为1 ～ 255个字符。不填默认为空字符串。
     */
    readonly description: string;
    /**
     * 健康检查的域名。只有 HealthCheckProtocol 设置为 HTTP 时，该参数才生效。要求如下：需至少包含一个‘.’，且不允许以‘.’开头结尾。单个字符串由字母、数字、‘-’、‘.’字符组成，‘-’不得出现在字符串的头部或尾部。长度限制为1 ～ 128个字符。不填则此参数为空，负载均衡默认使用各后端服务器的私网IP地址进行健康检查。
     */
    readonly healthCheckDomain: string;
    /**
     * 健康检查正常的HTTP状态码。多个状态码用以半角逗号分隔，只有 HealthCheckProtocol 设置为 HTTP 时，该参数才生效。取值如下：http*2xx（默认）。http*3xx（默认） 。http*4xx 。http*5xx 。
     */
    readonly healthCheckHttpCode: string;
    /**
     * 健康检查HTTP协议版本。只有 HealthCheckProtocol 设置为 HTTP 时，该参数才生效。取值如下：HTTP1.0（默认值）。HTTP1.1。
     */
    readonly healthCheckHttpVersion: string;
    /**
     * 执行健康检查的时间间隔，默认为2，取值1-300s 。
     */
    readonly healthCheckInterval: number;
    /**
     * 健康检查的方法。只有 HealthCheckProtocol 设置为 HTTP 时，该参数才生效。取值如下：GETHEAD（默认方法）
     */
    readonly healthCheckMethod: string;
    /**
     * 健康检查端口。取值如下：0：使用后端服务器端口进行健康检查。1-65535：使用您指定的端口进行健康检查。
     */
    readonly healthCheckPort: number;
    /**
     * 健康检查的协议。HTTPTCP。
     */
    readonly healthCheckProtocol: string;
    /**
     * 健康检查模板 ID。
     */
    readonly healthCheckTemplateId: string;
    /**
     * 健康检查模板的名称。不能以http://或https://开头。必须以字母或中文开头，可包含数字、点（.）、下划线（_）和短横线（-）。长度限制在1～128字符之间。
     */
    readonly healthCheckTemplateName: string;
    /**
     * 健康检查的响应超时时间，默认为2，取值1-60s 。
     */
    readonly healthCheckTimeout: number;
    /**
     * 健康检查的路径。只有 HealthCheckProtocol 设置为 HTTP 时，该参数才生效。要求如下：必须以字符‘/’开头。仅包含字母、数字、‘-’、‘_’、‘/’、‘.’、‘%’、‘?’、‘#’、‘&’、‘＝’字符。长度限制为1 ～ 128个字符。不填默认为“/”。
     */
    readonly healthCheckUri: string;
    /**
     * 健康检查的健康阈值，默认为3，取值2-10次。
     */
    readonly healthyThreshold: number;
    /**
     * Uniquely identifies the resource.
     */
    readonly id: string;
    /**
     * 健康检查模板所属项目名称。
     */
    readonly projectName: string;
    /**
     * 健康检查模板关联的标签列表。
     */
    readonly tags: outputs.alb.GetHealthCheckTemplateTag[];
    /**
     * 健康检查的不健康阈值，默认为3，取值2-10次。
     */
    readonly unhealthyThreshold: number;
    /**
     * 健康检查模版最近一次的操作时间。
     */
    readonly updateTime: string;
}
/**
 * Data Source schema for Volcengine::ALB::HealthCheckTemplate
 */
export function getHealthCheckTemplateOutput(args: GetHealthCheckTemplateOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetHealthCheckTemplateResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("volcenginecc:alb/getHealthCheckTemplate:getHealthCheckTemplate", {
        "id": args.id,
    }, opts);
}

/**
 * A collection of arguments for invoking getHealthCheckTemplate.
 */
export interface GetHealthCheckTemplateOutputArgs {
    /**
     * Uniquely identifies the resource.
     */
    id: pulumi.Input<string>;
}
