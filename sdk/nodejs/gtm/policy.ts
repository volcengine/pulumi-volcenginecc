// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Scheduling policy configuration under the GTM instance, identified by GtmId and PolicyType, and maintained via UpdatePolicy.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as volcenginecc from "@volcengine/pulumi-volcenginecc";
 *
 * const gTMPolicyDemo = new volcenginecc.gtm.Policy("GTMPolicyDemo", {
 *     gtmId: "003cd3af-dc55-xxxxx-xxxxxx-41470d406367",
 *     policyType: "perf",
 *     targets: [{
 *         pool_id: "ad65b9fb-e8f5-xxxxx-xxxxxx-bc4f2741cd66",
 *     }],
 *     alarmOnly: false,
 *     routingMode: "geo-lb",
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import volcenginecc:gtm/policy:Policy example "gtm_id|policy_type"
 * ```
 */
export class Policy extends pulumi.CustomResource {
    /**
     * Get an existing Policy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: PolicyState, opts?: pulumi.CustomResourceOptions): Policy {
        return new Policy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'volcenginecc:gtm/policy:Policy';

    /**
     * Returns true if the given object is an instance of Policy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Policy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Policy.__pulumiType;
    }

    /**
     * If the current address pool set is unavailable, Cloud Scheduling GTM only triggers an alert notification and does not automatically switch to an available address pool. true: Cloud Scheduling GTM only triggers an alert notification. false: Cloud Scheduling GTM automatically switches to an available address pool
     */
    declare public readonly alarmOnly: pulumi.Output<boolean>;
    /**
     * UUID of the GTM instance. You can obtain GtmId through the ListGtms API
     */
    declare public readonly gtmId: pulumi.Output<string>;
    /**
     * Routing mode for intelligent routing strategy. perf: Performance first. capacity: Capacity first. feedback: Load feedback
     */
    declare public /*out*/ readonly perfMode: pulumi.Output<string>;
    /**
     * Strategy type. geo: Basic routing. perf: Intelligent routing
     */
    declare public readonly policyType: pulumi.Output<string>;
    /**
     * Routing mode. The parameter values are: lb: Based on load balancing, user traffic is routed proportionally to different IDCs. geo: Based on the user's geographic location and operator, user traffic is routed to the nearest IDC on the same operator's network. geo-lb (default): First, based on the user's geographic location and operator, user traffic is routed to the nearest IDC access line on the same operator's network. Then, based on load balancing, user traffic is routed proportionally to multiple IDCs
     */
    declare public readonly routingMode: pulumi.Output<string>;
    /**
     * Statistical information about address status
     */
    declare public /*out*/ readonly statistics: pulumi.Output<outputs.gtm.PolicyStatistics>;
    /**
     * Traffic target for intelligent routing strategy
     * Important Note: When using SetNestedAttribute, you must fully define all attributes of its nested structure. Incomplete definitions may cause Terraform to detect unexpected differences during plan comparison, triggering unnecessary resource updates and affecting resource stability and predictability.
     */
    declare public readonly targets: pulumi.Output<outputs.gtm.PolicyTarget[]>;

    /**
     * Create a Policy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: PolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: PolicyArgs | PolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as PolicyState | undefined;
            resourceInputs["alarmOnly"] = state?.alarmOnly;
            resourceInputs["gtmId"] = state?.gtmId;
            resourceInputs["perfMode"] = state?.perfMode;
            resourceInputs["policyType"] = state?.policyType;
            resourceInputs["routingMode"] = state?.routingMode;
            resourceInputs["statistics"] = state?.statistics;
            resourceInputs["targets"] = state?.targets;
        } else {
            const args = argsOrState as PolicyArgs | undefined;
            if (args?.gtmId === undefined && !opts.urn) {
                throw new Error("Missing required property 'gtmId'");
            }
            if (args?.policyType === undefined && !opts.urn) {
                throw new Error("Missing required property 'policyType'");
            }
            resourceInputs["alarmOnly"] = args?.alarmOnly;
            resourceInputs["gtmId"] = args?.gtmId;
            resourceInputs["policyType"] = args?.policyType;
            resourceInputs["routingMode"] = args?.routingMode;
            resourceInputs["targets"] = args?.targets;
            resourceInputs["perfMode"] = undefined /*out*/;
            resourceInputs["statistics"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Policy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Policy resources.
 */
export interface PolicyState {
    /**
     * If the current address pool set is unavailable, Cloud Scheduling GTM only triggers an alert notification and does not automatically switch to an available address pool. true: Cloud Scheduling GTM only triggers an alert notification. false: Cloud Scheduling GTM automatically switches to an available address pool
     */
    alarmOnly?: pulumi.Input<boolean | undefined>;
    /**
     * UUID of the GTM instance. You can obtain GtmId through the ListGtms API
     */
    gtmId?: pulumi.Input<string | undefined>;
    /**
     * Routing mode for intelligent routing strategy. perf: Performance first. capacity: Capacity first. feedback: Load feedback
     */
    perfMode?: pulumi.Input<string | undefined>;
    /**
     * Strategy type. geo: Basic routing. perf: Intelligent routing
     */
    policyType?: pulumi.Input<string | undefined>;
    /**
     * Routing mode. The parameter values are: lb: Based on load balancing, user traffic is routed proportionally to different IDCs. geo: Based on the user's geographic location and operator, user traffic is routed to the nearest IDC on the same operator's network. geo-lb (default): First, based on the user's geographic location and operator, user traffic is routed to the nearest IDC access line on the same operator's network. Then, based on load balancing, user traffic is routed proportionally to multiple IDCs
     */
    routingMode?: pulumi.Input<string | undefined>;
    /**
     * Statistical information about address status
     */
    statistics?: pulumi.Input<inputs.gtm.PolicyStatistics | undefined>;
    /**
     * Traffic target for intelligent routing strategy
     * Important Note: When using SetNestedAttribute, you must fully define all attributes of its nested structure. Incomplete definitions may cause Terraform to detect unexpected differences during plan comparison, triggering unnecessary resource updates and affecting resource stability and predictability.
     */
    targets?: pulumi.Input<pulumi.Input<inputs.gtm.PolicyTarget>[] | undefined>;
}

/**
 * The set of arguments for constructing a Policy resource.
 */
export interface PolicyArgs {
    /**
     * If the current address pool set is unavailable, Cloud Scheduling GTM only triggers an alert notification and does not automatically switch to an available address pool. true: Cloud Scheduling GTM only triggers an alert notification. false: Cloud Scheduling GTM automatically switches to an available address pool
     */
    alarmOnly?: pulumi.Input<boolean | undefined>;
    /**
     * UUID of the GTM instance. You can obtain GtmId through the ListGtms API
     */
    gtmId: pulumi.Input<string>;
    /**
     * Strategy type. geo: Basic routing. perf: Intelligent routing
     */
    policyType: pulumi.Input<string>;
    /**
     * Routing mode. The parameter values are: lb: Based on load balancing, user traffic is routed proportionally to different IDCs. geo: Based on the user's geographic location and operator, user traffic is routed to the nearest IDC on the same operator's network. geo-lb (default): First, based on the user's geographic location and operator, user traffic is routed to the nearest IDC access line on the same operator's network. Then, based on load balancing, user traffic is routed proportionally to multiple IDCs
     */
    routingMode?: pulumi.Input<string | undefined>;
    /**
     * Traffic target for intelligent routing strategy
     * Important Note: When using SetNestedAttribute, you must fully define all attributes of its nested structure. Incomplete definitions may cause Terraform to detect unexpected differences during plan comparison, triggering unnecessary resource updates and affecting resource stability and predictability.
     */
    targets?: pulumi.Input<pulumi.Input<inputs.gtm.PolicyTarget>[] | undefined>;
}
